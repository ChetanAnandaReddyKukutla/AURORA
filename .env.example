# Aurora Apparel - Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=production

# Session Configuration (optional - for production use a secret key)
SESSION_SECRET=your-secret-key-here-change-in-production

# CORS Configuration (optional - add allowed origins for production)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
